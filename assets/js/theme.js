(function () { "use strict"; const d = document.documentElement, s = localStorage; const get = () => d.getAttribute("data-theme") || s.getItem("theme") || "light"; const update = () => { const t = get(), isDark = t === "dark", txt = isDark ? "Light Mode" : "Dark Mode", cls = isDark ? "fa-solid fa-sun" : "fa-solid fa-moon"; document.querySelectorAll("#theme-toggle, #mobile-theme-toggle, #footer-theme-btn").forEach(b => { const i = b.querySelector("i"), sp = b.querySelector("span"); if (i && i.className !== cls) i.className = cls; if (sp && sp.textContent !== txt) sp.textContent = txt; if (b.getAttribute("data-theme-text") !== txt) b.setAttribute("data-theme-text", txt) }) }; const set = t => { d.setAttribute("data-theme", t); s.setItem("theme", t); update() }; d.setAttribute("data-theme", get()); window.addEventListener("DOMContentLoaded", () => { update(); document.addEventListener("click", e => { const b = e.target.closest("#theme-toggle, #mobile-theme-toggle, #footer-theme-btn"); if (b) { e.preventDefault(); set(get() === "dark" ? "light" : "dark") } }); new MutationObserver(update).observe(document.body, { childList: !0, subtree: !0 }) }) })();